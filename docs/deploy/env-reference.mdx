---
title: Environment Variables
description: Complete reference for every environment variable in LaunchKit.
---

# Environment Variables Reference

All variables live in `.env.local` at the monorepo root. Copy `.env.example` to get started.

```bash
cp .env.example .env.local
```

<Note>
  Variables prefixed with `NEXT_PUBLIC_` are exposed to the browser. Never put secret keys in `NEXT_PUBLIC_` variables.
</Note>

---

## Required Variables

The minimum set to get LaunchKit running:

| Variable | Required | Description |
|---|---|---|
| `DATABASE_URL` | Yes | Neon connection string |
| `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` | Yes | Clerk public key |
| `CLERK_SECRET_KEY` | Yes | Clerk secret key |
| `CLERK_WEBHOOK_SECRET` | Yes | Clerk webhook signing secret |
| `STRIPE_SECRET_KEY` | Yes | Stripe secret key |
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | Yes | Stripe public key |
| `STRIPE_WEBHOOK_SECRET` | Yes | Stripe webhook signing secret |
| `RESEND_API_KEY` | Yes | Resend API key |
| `EMAIL_FROM` | Yes | Sender name + email |
| `NEXT_PUBLIC_APP_URL` | Yes | Full URL of your app |

---

## Database

```env
# Neon Postgres — sslmode=require is mandatory
DATABASE_URL=postgresql://user:password@ep-xxx.us-west-2.aws.neon.tech/neondb?sslmode=require
```

---

## Clerk (Auth)

```env
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/auth/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/auth/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/portal
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/portal
CLERK_WEBHOOK_SECRET=whsec_xxx
```

---

## Stripe (Billing)

```env
# Keys
BILLING_PROVIDER=stripe
STRIPE_SECRET_KEY=sk_test_xxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Your SaaS subscription plan price IDs
STRIPE_PRO_MONTHLY_PRICE_ID=price_xxx
STRIPE_PRO_YEARLY_PRICE_ID=price_xxx
STRIPE_TEAM_MONTHLY_PRICE_ID=price_xxx
STRIPE_TEAM_YEARLY_PRICE_ID=price_xxx
```

---

## Email (Resend)

```env
RESEND_API_KEY=re_xxx
EMAIL_FROM=YourApp <hello@yourdomain.com>
```

---

## Analytics

<CodeGroup>
```env PostHog
NEXT_PUBLIC_POSTHOG_KEY=phc_xxx
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
POSTHOG_API_KEY=phx_xxx   # optional, for server-side events
```

```env Plausible (alternative)
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=yourdomain.com
```
</CodeGroup>

---

## App

```env
NEXT_PUBLIC_APP_URL=http://localhost:3000    # https://yourdomain.com in production
NEXT_PUBLIC_APP_NAME=YourApp
NEXT_PUBLIC_TEAMS_ONLY=false   # true = B2B mode, removes personal account features
DEMO_MODE=false
NEXT_PUBLIC_DEMO_MODE=false
```

---

## AI Providers (Optional)

Add any providers you want to use in your SaaS:

```env
OPENAI_API_KEY=sk-xxx
ANTHROPIC_API_KEY=sk-ant-xxx
GOOGLE_GENERATIVE_AI_API_KEY=xxx
PERPLEXITY_API_KEY=pplx-xxx
GROQ_API_KEY=gsk_xxx
```

---

## Storage (Optional)

For file uploads:

```env
STORAGE_PROVIDER=vercel-blob   # vercel-blob | s3 | gcs
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx
```

---

## Sentry (Optional)

For error tracking:

```env
NEXT_PUBLIC_SENTRY_DSN=https://xxx@sentry.io/xxx
SENTRY_DSN=https://xxx@sentry.io/xxx
SENTRY_ORG=your-org
SENTRY_PROJECT=your-project
SENTRY_AUTH_TOKEN=xxx
```

---

## Environment Precedence

Next.js loads env files in this order (highest priority first):

1. `.env.local` — your local overrides (gitignored)
2. `.env.[NODE_ENV]` — e.g. `.env.production`
3. `.env` — committed defaults

Always put your values in `.env.local`.
